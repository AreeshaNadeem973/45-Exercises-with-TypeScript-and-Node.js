interface itCourse {
    courseName : string;
    location: string;
    onSiteStudents: number;
}

let itCourse = {
    courseName : "Typescript and JacaScript",
    location : "Governor House Sindh",
    OnSiteStudents : 50000
};

console.log(itCourse);