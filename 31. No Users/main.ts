


let username: string[] = ["areeba", "admin", "areesha", "laiba", "akeela"]

while (username.length > 0) {
    username.pop()
}

if (username.length === 0){
    console.log("We need to find some users!");
    
}